import { SubmitSignedTransactionResponse, Wallet, XRPAmount } from "xpring-common-js";
import { NetworkClient } from "./network-client";
export declare class XpringClientErrorMessages {
    static readonly malformedResponse = "Malformed Response.";
    static readonly signingFailure = "Unable to sign the transaction";
}
declare class XpringClient {
    private readonly networkClient;
    static xpringClientWithEndpoint(grpcURL: string): XpringClient;
    constructor(networkClient: NetworkClient);
    getBalance(address: string): Promise<XRPAmount>;
    send(sender: Wallet, amount: XRPAmount, destination: string): Promise<SubmitSignedTransactionResponse>;
    private getAccountInfo;
    private getFee;
}
export default XpringClient;
