export interface WalletGenerationResult {
    wallet: Wallet;
    mnemonic: string;
    derivationPath: string;
}
declare class Wallet {
    private readonly publicKey;
    private readonly privateKey;
    static getDefaultDerivationPath(): string;
    static generateRandomWallet(entropy?: string | undefined): WalletGenerationResult | undefined;
    static generateWalletFromMnemonic(mnemonic: string, derivationPath?: string): Wallet | undefined;
    static generateWalletFromSeed(seed: string): Wallet | undefined;
    constructor(publicKey: string, privateKey: string);
    getPublicKey(): string;
    getPrivateKey(): string;
    getAddress(): string;
    sign(hex: string): string | undefined;
    verify(message: string, signature: string): boolean;
    private static hexFromBuffer;
}
export default Wallet;
