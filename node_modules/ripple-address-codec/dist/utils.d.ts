declare function seqEqual(arr1: any, arr2: any): boolean;
declare function isSequence(val: any): boolean;
/**
* Concatenates all `arguments` into a single array. Each argument can be either
* a single element or a sequence, which has a `length` property and supports
* element retrieval via sequence[ix].
*
* > concatArgs(1, [2, 3], Buffer.from([4,5]), new Uint8Array([6, 7]));
*  [1,2,3,4,5,6,7]
*
* @return {Array} - concatenated arguments
*/
declare function concatArgs(): any[];
declare function isSet(o: any): boolean;
